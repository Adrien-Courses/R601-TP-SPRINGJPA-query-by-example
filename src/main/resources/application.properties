spring.application.name=jpa-spring-enterprise-architecture-complement
server.port=4546

# Configuration MySQL
spring.datasource.url=jdbc:mysql://localhost:3310/jpa-spring-query-by-example
spring.datasource.username=root
spring.datasource.password=password

# Configuration JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# 1. Lancer une fois avec les deux commentés (pour initialiser la BDD)
# 2. Lanber une fois en décommentant spring.sql.init.mode=always
# 3. Remplacer always par never pour les prochain lancement

#spring.sql.init.mode=always
#spring.sql.init.mode=always=never